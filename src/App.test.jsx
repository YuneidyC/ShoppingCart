import { describe, it, expect } from 'vitest';
// import { render } from '@testing-library/react';
import App from './App';

describe('true', () => {
    it('true', () => {
        const bool = true;
        expect(bool).toBe(true);
    });
});
